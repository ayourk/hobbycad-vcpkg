# Qt 6 SVG Module - now built as part of qtbase
#
# This is a meta-port for backwards compatibility.
# Qt6Svg is now built together with qtbase to avoid Qt6BuildInternals complexity.

set(VCPKG_POLICY_EMPTY_PACKAGE enabled)
